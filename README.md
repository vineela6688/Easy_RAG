This simple RAG apllication scrapes the data from a given browser and answers the queries.If the data is not available it uses a custom prompt to show the data is not available.
It uses Open-AI embedding model and LLM
It used chroma DB as vector store

